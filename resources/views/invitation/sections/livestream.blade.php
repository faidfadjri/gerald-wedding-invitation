<section id="livestream">
    <img src="{{ $config['assets']['dreamy'] }}" alt="" class="ls-bg parallax-ornament" data-parallax-speed="-0.15"
        aria-hidden="true">
    <div class="ls-inner">
        <img src="{{ $config['assets']['leaf_2'] }}" alt="" class="ornament-float"
            style="width:90px;opacity:0.55;margin-bottom:1rem;" aria-hidden="true">
        <h3 class="reveal">Live Streaming</h3>
        <p class="reveal delay-1">{{ $config['streaming_note'] }}</p>
        <a href="{{ $config['streaming_url'] }}" class="btn-outline reveal delay-2">📹 go to streaming</a>
    </div>
</section>